import type { NextRequest } from 'next/server';
import { POST as marketerPOST } from '../marketer/route';

export const maxDuration = 60;

/* ============================================================================
 * /api/ai/agent — منصة افلييت: لا بيع مباشر (منذ إزالة السلة والدفع).
 *
 * كان هذا المسار «مندوب مبيعات» يسجّل طلبات COD مباشرة من الشات. منذ تحول
 * المنصة إلى منصة تسويق بالعمولة فقط، أصبح أي بيع مباشر ممنوع — بما فيه
 * عبر الـ API. الحل الأنظف: المسار يفوّض كامل منطقه إلى «مساعد المسوقين»
 * (/api/ai/agent → /api/ai/marketer) الذي:
 *   • يدوّر منتجات ويرشّحها بالعمولة والدراسة التسويقية
 *   • يجاوب عن العمولات (1–10 د.ك — والمسوق يختار) والمحفظة والدعاية والمواسم
 *   • يوجّه الزائر للتسجيل كمُسوّق — بدون أي مسار إنشاء طلبات
 * الشكل القديم للطلب (draft / customerConfirmed / lastOfferedIds) يُتجاهَل
 * بأمان لأن مساعد المسوقين يقرأ lang + messages فقط.
 * ========================================================================== */

export async function POST(req: NextRequest) {
  return marketerPOST(req);
}
